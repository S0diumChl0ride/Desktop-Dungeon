// initializes character creation, screens and stats.
const titleScreen = document.getElementById('titleScreen');
let playerName;

//Verifies that you clicked the screen. Will use this to load in next screen, player naming screen.
titleScreen.addEventListener('click', nextScreen);

function nextScreen(){
    console.log("Its working!")
};

console.log(inputPlayerName);

//Ask for player name
playerName = Element.getElementById('inputPlayerName');








